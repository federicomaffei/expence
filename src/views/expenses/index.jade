extends ../layout/layout
block content
    h2 Here are your expenses:
    ul
        each expense in expenses
            li #{expense.amount} #{expense.currency} for #{expense.reason} at #{expense.creationDate}